const ctrlWrapper = require("./ctrlWrapper");
const { addContactValidator, updateContactValidator } = require("./validator");

module.exports = {
  ctrlWrapper,
  addContactValidator,
  updateContactValidator,
};
